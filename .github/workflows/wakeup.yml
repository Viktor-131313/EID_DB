name: Wake Up Render
on:
  schedule:
    - cron: '*/13 * * * *'  # КАВЫЧКИ важны!
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          curl -s https://eid-praktis-id.onrender.com/
          echo "Pinged at $(date)"
