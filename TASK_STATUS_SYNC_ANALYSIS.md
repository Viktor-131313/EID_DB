# 📊 Анализ возможностей автоматического обновления статусов задач

> **Статус:** ⏸️ Отложено до перехода на новый таск-менеджер (январь 2026)
> 
> **Решение:** Пока статусы проставляются вручную. После перехода на новый таск-менеджер будет пересмотрена возможность автоматизации.

## Текущая ситуация

- ✅ Есть ссылки на задачи в таск-менеджере (сохраняются в `taskManagerLink`)
- ✅ Есть номера задач
- ❌ Нет API у таск-менеджера
- ❓ Нужно автоматически обновлять статусы задач (In Progress, Review, Done и т.д.)

## Возможные решения (без API)

### 1. 🔍 Web Scraping (Парсинг HTML страницы)

**Как работает:**
- Открываем URL задачи из `taskManagerLink`
- Парсим HTML страницы
- Извлекаем статус (например, из элемента с классом `.status` или `.task-status`)
- Обновляем статус в нашей базе данных

**Плюсы:**
- ✅ Относительно просто реализовать
- ✅ Не требует изменений на стороне таск-менеджера
- ✅ Можно запускать по расписанию (cron job)

**Минусы:**
- ❌ Требует авторизацию (нужны cookies/сессия)
- ❌ Хрупко - ломается при изменении структуры HTML
- ❌ Может нарушать Terms of Service таск-менеджера
- ❌ Медленнее, чем API
- ❌ Нужно обрабатывать разные страницы для разных типов задач

**Реализуемость:** ⚠️ Средняя сложность (3-4 часа разработки)

---

### 2. 🤖 Browser Automation (Puppeteer/Playwright)

**Как работает:**
- Используем Puppeteer или Playwright для управления браузером
- Автоматически входим в систему таск-менеджера
- Навигируем к странице задачи
- Извлекаем статус из DOM
- Обновляем в нашей базе

**Плюсы:**
- ✅ Работает как настоящий браузер (JS выполняется)
- ✅ Можно обрабатывать динамические страницы (React/Vue)
- ✅ Можно делать скриншоты для отладки

**Минусы:**
- ❌ Очень ресурсоемко (нужен браузер на сервере)
- ❌ Требует хранить логин/пароль для таск-менеджера
- ❌ Медленно (каждый запрос занимает несколько секунд)
- ❌ На Render.com free tier может не работать (нужен headless browser)
- ❌ Сложнее в настройке и поддержке

**Реализуемость:** ⚠️ Высокая сложность (6-8 часов разработки)

---

### 3. 🔌 Browser Extension / UserScript

**Как работает:**
- Создаем расширение для браузера или userscript
- Пользователь устанавливает его
- При открытии страницы задачи расширение извлекает статус
- Отправляет статус на наш сервер через API

**Плюсы:**
- ✅ Работает от имени пользователя (авторизация уже есть)
- ✅ Можно обновлять статусы в реальном времени
- ✅ Не требует парсинга на сервере

**Минусы:**
- ❌ Требует установки расширения каждым пользователем
- ❌ Только для активных пользователей (кто открыл страницу)
- ❌ Нужно поддерживать расширение для разных браузеров
- ❌ Пользователь должен быть авторизован в таск-менеджере

**Реализуемость:** ⚠️ Средняя сложность (4-6 часов разработки)

---

### 4. 🔄 Ручное обновление через UI

**Как работает:**
- Пользователь вручную обновляет статусы в нашем интерфейсе
- Можно добавить кнопку "Обновить статус" рядом со ссылкой на таск-менеджер

**Плюсы:**
- ✅ Просто реализовать (уже есть)
- ✅ 100% надежно
- ✅ Не требует парсинга

**Минусы:**
- ❌ Не автоматическое
- ❌ Требует ручного действия от пользователя

**Реализуемость:** ✅ Уже реализовано

---

### 5. 📊 Парсинг через RSS/Atom Feed (если есть)

**Как работает:**
- Если таск-менеджер предоставляет RSS/Atom feed для задач
- Парсим feed и извлекаем статусы

**Плюсы:**
- ✅ Стандартизированный формат
- ✅ Легко парсить
- ✅ Не требует авторизации (обычно)

**Минусы:**
- ❌ Маловероятно, что есть (пользователь сказал, что API нет)
- ❌ Может не включать статусы

**Реализуемость:** ❓ Зависит от наличия feed

---

## Рекомендация

### Вариант A: Web Scraping (Простой) - РЕКОМЕНДУЕТСЯ для начала

**Что нужно:**

1. **Библиотека для HTTP запросов:**
   - `axios` (уже есть в проекте)
   - `cheerio` (для парсинга HTML) - нужно добавить

2. **Авторизация:**
   - Нужны credentials (логин/пароль) для таск-менеджера
   - Нужно будет реализовать логин и сохранение сессии (cookies)
   - Или использовать Basic Auth, если поддерживается

3. **Реализация:**
   ```javascript
   // Псевдокод
   async function syncTaskStatus(taskManagerLink, taskId) {
     // 1. Получить страницу задачи (с авторизацией)
     const html = await fetchTaskPage(taskManagerLink);
     
     // 2. Парсить HTML
     const $ = cheerio.load(html);
     const status = $('.task-status').text().trim(); // или другой селектор
     
     // 3. Маппинг статусов (из таск-менеджера в наш формат)
     const ourStatus = mapStatus(status); // "В работе" -> "In Progress"
     
     // 4. Обновить задачу в БД
     await updateTaskStatus(taskId, ourStatus);
   }
   ```

4. **Расписание:**
   - Можно запускать по cron (например, раз в час)
   - Или по кнопке "Обновить статусы"

**Требования для реализации:**
- ✅ URL задачи (есть в `taskManagerLink`)
- ❓ Логин/пароль для таск-менеджера (нужно уточнить)
- ❓ Структура HTML страницы (нужно изучить)
- ❓ Как авторизовываться в таск-менеджере (форма логина? Basic Auth?)

---

### Вариант B: Browser Extension (Более надежный)

Если web scraping не работает из-за сложной авторизации или динамического контента, можно создать простое расширение для браузера, которое будет отправлять статус на наш сервер.

---

## Вопросы для уточнения

Чтобы выбрать лучший подход, нужно знать:

1. **Какой таск-менеджер используется?** (название/URL)
2. **Требуется ли авторизация для просмотра задач?** (нужен ли логин/пароль)
3. **Можно ли получить доступ к задачам без авторизации?** (публичные ссылки?)
4. **Как выглядит страница задачи?** (можно ли просто открыть URL и увидеть статус?)
5. **Какой HTML-селектор содержит статус?** (класс, ID элемента)
6. **Можно ли использовать headless browser на сервере?** (Puppeteer/Playwright)
7. **Какие статусы используются в таск-менеджере?** (нужен маппинг на наши статусы)

---

## Следующие шаги

1. **Изучить структуру страницы задачи** - открыть несколько ссылок и посмотреть HTML
2. **Определить способ авторизации** - как войти в систему
3. **Создать прототип парсера** - извлечь статус с одной страницы
4. **Реализовать маппинг статусов** - сопоставить статусы таск-менеджера с нашими
5. **Добавить cron job** - автоматическое обновление по расписанию

---

**Какой вариант вам больше подходит? Нужна ли помощь с изучением структуры страницы?**

