# Praktis ID Dashboard

Дашборд для мониторинга внедрения электронной исполнительной документации Praktis ID на пилотных объектах.

## 📋 Описание проекта

Веб-приложение для отслеживания прогресса согласования актов исполнительной документации на различных объектах. Система позволяет группировать объекты по контейнерам, отслеживать статистику по каждому контейнеру, сравнивать статистику между планерками и управлять задачами разработки.

## 🎯 Основные возможности

### 🔐 Система аутентификации

- **Режим просмотра (View-only)**: По умолчанию приложение открывается в режиме просмотра
  - Просмотр всех данных, экспорт в PDF
  - Открытие объектов в режиме просмотра
  - Развертывание статистики и сравнение планерок
  - **Без возможности редактирования, добавления или удаления данных**
- **Авторизация**: Клик по favicon (логотип) в хедере открывает модальное окно входа
  - Логин: `admin`
  - Пароль: `Admin2026!`
  - После авторизации доступны все функции редактирования
  - В хедере появляется кнопка "Выйти" для выхода из системы

### Управление контейнерами объектов

- **Создание контейнеров**: Нажмите клавишу `Insert` для создания нового контейнера (только для авторизованных пользователей)
- **Переименование заголовка**: Двойной клик по заголовку "Объекты" позволяет переименовать его (например, "Объекты Сетл")
- **Переименование контейнеров**: Двойной клик по названию контейнера открывает режим редактирования (только для авторизованных пользователей)
- **Перемещение контейнеров**: Кнопки "↑" и "↓" слева от каждого контейнера позволяют изменить порядок отображения (только для авторизованных пользователей)
  - Кнопки автоматически отключаются, если контейнер уже первый или последний
  - Порядок сохраняется в базе данных и восстанавливается при перезагрузке

### Управление объектами

- **Добавление объектов**: Кнопка "Добавить объект" находится в каждом контейнере (только для авторизованных пользователей)
- **Просмотр объектов**: Клик по карточке объекта открывает детальное модальное окно (доступно в режиме просмотра)
- **Редактирование объектов**: В модальном окне можно редактировать все данные объекта (только для авторизованных пользователей)
- **Удаление объектов**: Доступно в модальном окне редактирования (только для авторизованных пользователей)
- **Фото объектов**: Возможность загрузить JPEG или PNG фото объекта (отображается на карточке над названием)
  - Фото увеличивает высоту карточки, но не ширину
  - Фото сохраняется в base64 формате
  - **Редактирование фото**: Для админов доступны кнопки "Изменить фото" и "Удалить фото" с кастомными подсказками
- **Статус объекта**: Отображается на карточке с черной рамкой и подписью "Статус:"
- **Метрики исполнительности**: На карточках объектов отображаются:
  - Процент готовности актов (готовность = сгенерировано / должно быть из Айконы)
  - Отставание (сколько актов еще нужно сгенерировать)
  - Визуальные индикаторы: зеленый (хорошо), желтый (требует внимания), красный (критично)
  - Значок критического объекта (пульсирующая красная иконка) для объектов с готовностью < 70%

### Структура данных объекта

Каждый объект содержит:

1. **Название объекта** - основное название
2. **Секция/подрядчик** - дополнительное описание
3. **Фото объекта** - JPEG или PNG изображение (опционально)
4. **Статус** - краткий текстовый статус объекта (отображается на карточке с черной рамкой)
5. **ID объекта в Айконе** - идентификатор для синхронизации с внешней системой (только для админов)
6. **Сгенерированных актов** - раскрывающаяся секция со списком СМР:
   - Каждый СМР имеет название, количество актов (Praktis ID) и общее количество (СЗ Icona)
   - Заголовки столбцов: "СМР", "Praktis ID", "СЗ Icona"
   - Двойной клик по СМР позволяет переименовать
   - Нажатие Delete удаляет СМР (с кастомным подтверждением)
   - Кнопка "+ Добавить СМР" создает новый СМР сразу во всех секциях актов
   - Кнопка "Забрать данные" для ручной синхронизации с Aikona API (только для админов)
7. **Актов отправлено на согласование** - раскрывающаяся секция:
   - Показывает СМР из сгенерированных
   - Количество ограничено максимумом из сгенерированных
8. **Актов согласовано** - раскрывающаяся секция со списком СМР
9. **Актов отклонено** - раскрывающаяся секция со списком СМР
10. **Актов подписано** - раскрывающаяся секция со списком СМР
11. **Блокирующие факты** - список сотрудников, требующих вовлечения руководства
    - Отображается на карточке объекта (только просмотр)
    - Редактирование только в модальном окне

### Статистика

- **Общая статистика**: Отображается вверху страницы, считает все объекты из всех контейнеров
  - Всего объектов
  - Сгенерировано актов
  - Отправлено на согласование
  - Согласовано (с процентом от отправленных)
  - Отклонено (с процентом от отправленных)
  - **Подписано** (с процентом от согласованных) ✨

- **Статистика контейнера**: Раскрывается кнопкой "Показать статистику" в заголовке каждого контейнера
  - Те же метрики, что и в общей статистике
  - Считает только объекты конкретного контейнера
  - **Процент сгенерированных**: Рассчитывается как процент сгенерированных актов от общего количества объектов

### Аналитика исполнительности и система алертов

- **Метрики на карточках объектов**:
  - **Процент готовности актов**: Рассчитывается как (сгенерировано / должно быть из Айконы) × 100%
  - **Отставание**: Количество актов, которые еще нужно сгенерировать
  - **Визуальные индикаторы статуса**:
    - 🟢 Зеленый (Хорошо): готовность ≥ 85%
    - 🟡 Желтый (Требует внимания): готовность 70-84%
    - 🔴 Красный (Критично): готовность < 70%
  - **Значок критического объекта**: Пульсирующая красная иконка для объектов с готовностью < 70%

- **Панель "Проблемы"**: Кнопка в хедере Dashboard (показывается только при наличии критических объектов)
  - Отображает количество критических объектов
  - При клике открывается модальное окно со списком проблемных объектов
  - Для каждого объекта показывается: название, контейнер, процент готовности, отставание
  - Клик по объекту в панели:
    - Закрывает панель проблем
    - Открывает модальное окно объекта
    - Автоматически раскрывает секцию "Сгенерированных актов"
    - Подсвечивает проблемную секцию (красная рамка, пульсирующая анимация)
    - Прокручивает к проблемной секции

### Статистика планерок

- **Создание снимков**: Ручное создание снимка текущего состояния для отслеживания изменений (только для авторизованных пользователей)
  - Кастомное модальное окно подтверждения перед созданием
  - Уведомление об успешном создании (автоматически исчезает через 3 секунды)
- **Сравнение статистики**: Сравнение текущего состояния с предыдущими планерками (доступно в режиме просмотра)
- **Визуализация**: График с отображением изменений (абсолютные значения и дельты)
  - Все цифры в диаграмме отображаются черным цветом
- **Фильтрация метрик**: Чекбоксы для выбора отображаемых типов актов
- **Таблица изменений**: Детальная таблица изменений по контейнерам и объектам
  - **Строка итогов**: Внизу таблицы отображается общая сумма изменений по каждому столбцу
  - Итоги отображаются как в веб-интерфейсе, так и в PDF экспорте
- **Выбор периода**: Возможность выбрать любую предыдущую планерку для сравнения
- **Отображение времени**: Дата и время создания снимка отображаются в графике
- **Удаление снимков**: Возможность удалить сохраненный снимок (только для авторизованных пользователей)
  - Кастомное модальное окно подтверждения с указанием даты снимка
  - Уведомление об успешном удалении

### 📄 Экспорт в PDF

- **Кнопка экспорта**: Кнопка "Экспорт в PDF" в хедере дашборда
- **Индикатор прогресса**: При экспорте отображается кастомное модальное окно с прогрессом генерации
  - Формат: "Идет генерация PDF X из Y..." (X - номер уже сгенерированной страницы)
  - Анимированные точки для визуализации процесса
  - Страницы скрыты от пользователя во время генерации
- **Титульная страница**: Первая страница PDF содержит изображение `PDF_first_list.png` с текущей датой в правом нижнем углу
- **Вторая страница**: 
  - Заголовок "Praktis ID"
  - Общая статистика (SummaryCards)
  - Статистика планерок (сравнение, графики, легенда с чекбоксами, изменения по контейнерам)
  - Все цифры в диаграмме отображаются черным цветом
  - Строка итогов в таблице изменений по контейнерам
  - Без сохраненных снимков статистики
- **Страницы объектов**: Каждый контейнер объектов выводится на отдельной странице
  - Плитки объектов одинакового размера на всех страницах
  - Фото объектов отображаются на плитках
  - Статус объекта с черной рамкой и подписью "Статус:"
- **Страницы задач**: Таблица "Задачи для разработчиков" занимает почти всю ширину страницы
  - 16 строк на странице, остальные переносятся на следующие страницы
  - Заголовки столбцов правильно выровнены над колонками
  - Заголовки "Дата обнаружения", "Статус", "Планируется устранить", "Критичность" выровнены по правому краю
  - Таблица автоматически переносится на следующие страницы при большом количестве строк

### Задачи для разработки

- **Управление задачами**: Таблица с задачами разработки
- **Свернутое состояние по умолчанию**: Секция задач по умолчанию свернута на веб-интерфейсе (кнопка развернуть/свернуть)
  - При экспорте в PDF все задачи всегда полностью развернуты
- **Структура задачи**:
  - ID (DEV-номер) - буквы "DEV-" фиксированы, вводится только номер
  - Описание задачи
  - Дата обнаружения (календарь)
  - Статус: To Do, In Progress, Review, Done
  - Планируемая дата исправления (месяц и год, опционально)
  - **Критичность**: Уровень важности задачи
    - ⚠️ **Критично** (красный) - критически важные задачи
    - ⚡ **Некритично** (желтый) - некритичные задачи
    - 💡 **Пожелания от пользователей** (зеленый) - запросы пользователей
  - **Ссылка на таск-менеджер**: Опциональное поле для ссылки на задачу в внешней системе
    - Отображается кнопкой со стрелкой в столбце "Действия"
    - Кнопка видна всем пользователям (не только админам)
    - Открывает ссылку в новой вкладке
- **Сортировка по критичности**: Задачи автоматически сортируются по уровню критичности (критичные → некритичные → пожелания)
- **Inline редактирование**: 
  - Статус, планируемая дата и критичность редактируются прямо в таблице (клик по ячейке) - только для авторизованных пользователей
- **Фильтрация**: По статусу и месяцу обнаружения
- **Удаление**: Через кнопку или клавишу Delete (с кастомным модальным окном подтверждения, только для авторизованных пользователей)
- **Добавление задач**: Кнопка "Добавить задачу" (только для авторизованных пользователей)

## 🏗️ Технологический стек

### Backend
- **Node.js** с **Express.js**
- **PostgreSQL** база данных (опционально, если установлена переменная `DATABASE_URL`)
- Гибридное хранение данных: PostgreSQL или JSON файлы (автоматическое определение)
- RESTful API
- CORS для работы с фронтендом
- Раздача статических файлов React в production
- **Интеграция с Aikona API**: Автоматическая синхронизация данных о выполненных работах
  - Ежедневная синхронизация в 7:00 (МСК) через cron job
  - Ручная синхронизация через кнопку "Забрать данные"
  - Логирование результатов синхронизации

### Frontend
- **React 18**
- Компонентная архитектура
- Axios для HTTP запросов
- Font Awesome для иконок
- CSS для стилизации (включая кастомные графики)
- **Адаптивная верстка**: Поддержка различных разрешений экранов
  - Мобильные устройства: 360px (оптимизированы плитки, убраны лишние отступы, скрыты кнопки перемещения контейнеров)
  - Планшеты: 768px
  - Десктопы: 1040px, 1366px, 1920px
- **Кастомные модальные окна**: Замена стандартных `alert` и `confirm` на стилизованные компоненты
  - `ConfirmModal` - для подтверждений действий
  - `ToastNotification` - для уведомлений (автоматически исчезают через 3 секунды)
- **Кастомные tooltips**: Все подсказки используют единый стиль
  - Белый текст на черном фоне
  - Закругленные края
  - Плавная анимация появления
  - Поддержка различных позиций (сверху, снизу, справа, слева)
  - Tooltips для кнопок фото отображаются снизу, чтобы не обрезаться контейнером

## 📁 Структура проекта

```
ID/
├── backend/                    # Node.js/Express API сервер
│   ├── server.js              # Основной файл сервера
│   ├── database/              # Модули работы с базой данных
│   │   ├── database.js        # Основные функции работы с PostgreSQL
│   │   ├── adapter.js         # Адаптер для выбора источника данных (БД или файлы)
│   │   ├── database-update-object.js  # Функции для обновления отдельных объектов
│   │   ├── schema.sql         # SQL схема базы данных
│   │   └── migrations/        # SQL миграции
│   ├── services/              # Сервисы
│   │   ├── aikona-sync.js     # Синхронизация с Aikona API
│   │   └── aikona-auto-sync.js # Автоматическая синхронизация
│   ├── data/                  # Папка для хранения данных (если не используется БД)
│   │   ├── objects.json       # JSON файл с объектами
│   │   ├── snapshots.json     # JSON файл со снимками статистики
│   │   ├── tasks.json         # JSON файл с задачами разработки
│   │   └── aikona-sync-log.json # Лог последней синхронизации с Aikona
│   ├── scripts/               # Вспомогательные скрипты
│   │   ├── migrate-local-data.js  # Миграция данных из JSON в PostgreSQL
│   │   └── download-production-data.js  # Скачивание данных с продакшена
│   └── package.json           # Зависимости бэкенда
│
├── frontend/                   # React приложение
│   ├── public/
│   │   ├── index.html         # Главная HTML страница
│   │   ├── favicon.png        # Иконка сайта (кликабельна для авторизации)
│   │   ├── favicon.ico        # Иконка сайта (альтернативная)
│   │   └── PDF_first_list.png # Изображение для титульной страницы PDF
│   ├── src/
│   │   ├── components/        # React компоненты
│   │   │   ├── Dashboard.js           # Главный дашборд
│   │   │   ├── Container.js           # Компонент контейнера
│   │   │   ├── ObjectsList.js         # Список объектов
│   │   │   ├── ObjectModal.js         # Модальное окно объекта
│   │   │   ├── SummaryCards.js        # Карточки статистики
│   │   │   ├── StatisticsWidget.js    # Виджет статистики планерок
│   │   │   ├── DevelopmentTasks.js    # Компонент задач разработки
│   │   │   ├── AuthModal.js           # Модальное окно авторизации
│   │   │   ├── ToastNotification.js   # Компонент уведомлений
│   │   │   ├── ConfirmModal.js        # Компонент подтверждений
│   │   │   ├── SyncLogModal.js        # Модальное окно логов синхронизации
│   │   │   ├── SnapshotScheduleModal.js # Модальное окно настройки расписания снимков
│   │   │   ├── AlertsPanel.js         # Панель критических объектов
│   │   │   └── Tooltip.js             # Кастомный компонент подсказок
│   │   ├── services/          # API сервисы
│   │   │   ├── api-containers.js      # API для контейнеров и объектов
│   │   │   └── api-tasks.js           # API для задач разработки
│   │   ├── utils/             # Утилиты
│   │   │   ├── pdfExport.js           # Экспорт в PDF
│   │   │   └── performanceMetrics.js  # Расчет метрик исполнительности
│   │   ├── App.js             # Главный компонент
│   │   └── index.js           # Точка входа
│   └── package.json           # Зависимости фронтенда
│
├── scripts/                    # Вспомогательные скрипты
│   ├── download-production-data.js  # Скачивание данных с продакшена
│   ├── check-production-data.js     # Проверка данных на продакшене
│   └── pre-commit-check.js          # Проверка данных перед коммитом
│
└── README.md                   # Этот файл
```

## 🚀 Установка и запуск

### Предварительные требования

- Node.js (версия 14 или выше)
- npm (обычно устанавливается вместе с Node.js)

### Установка

1. **Установка зависимостей бэкенда:**
   ```bash
   cd ID/backend
   npm install
   ```

2. **Установка зависимостей фронтенда:**
   ```bash
   cd ID/frontend
   npm install
   ```

### Запуск

#### Способ 1: Запуск в отдельных терминалах (рекомендуется)

**Терминал 1 - Бэкенд:**
```bash
cd ID/backend
npm start
```
Бэкенд запустится на `http://localhost:3001`

**Терминал 2 - Фронтенд:**
```bash
cd ID/frontend
npm start
```
Фронтенд откроется автоматически на `http://localhost:3000`

#### Способ 2: Использование скриптов

**Windows (PowerShell):**
```powershell
# Бэкенд
cd ID
.\start-backend.ps1

# Фронтенд (в другом терминале)
cd ID/frontend
npm start
```

**Linux/Mac/Git Bash:**
```bash
# Бэкенд
cd ID
./start-dev.sh
```

## 📡 API Endpoints

### Контейнеры

- `GET /api/containers` - Получить все контейнеры (сортировка по `display_order`)
- `POST /api/containers` - Создать новый контейнер
- `PUT /api/containers/:containerId` - Обновить контейнер (переименовать)
- `POST /api/containers/:containerId/move` - Переместить контейнер вверх или вниз (`direction: 'up' | 'down'`)
- `DELETE /api/containers/:containerId` - Удалить контейнер
- `GET /api/containers/:containerId/stats` - Получить статистику контейнера

### Объекты в контейнерах

- `GET /api/containers/:containerId/objects` - Получить объекты контейнера
- `POST /api/containers/:containerId/objects` - Создать объект в контейнере
- `PUT /api/containers/:containerId/objects/:objectId` - Обновить объект
- `DELETE /api/containers/:containerId/objects/:objectId` - Удалить объект
- `POST /api/containers/:containerId/objects/:objectId/sync-aikona` - Синхронизировать данные объекта из Aikona API

### Статистика

- `GET /api/stats` - Получить общую статистику по всем контейнерам
- `GET /api/health` - Проверка здоровья сервера

### Снимки статистики (Snapshots)

- `GET /api/snapshots` - Получить все снимки
- `POST /api/snapshots` - Создать новый снимок текущего состояния
- `GET /api/snapshots/latest/compare` - Сравнить текущее состояние с последним снимком
- `GET /api/snapshots/compare/:snapshotId` - Сравнить текущее состояние с указанным снимком

### Задачи разработки (Tasks)

- `GET /api/tasks` - Получить все задачи
- `POST /api/tasks` - Создать новую задачу
- `PUT /api/tasks/:taskId` - Обновить задачу
- `DELETE /api/tasks/:taskId` - Удалить задачу

### Интеграция с Aikona API

- `GET /api/aikona-sync-log` - Получить последний лог синхронизации с Aikona

## 💾 Структура данных

### База данных PostgreSQL

При наличии переменной окружения `DATABASE_URL` система автоматически использует PostgreSQL вместо JSON файлов.

**Таблицы:**
- `containers` - контейнеры объектов (id, name, display_order, created_at, updated_at)
- `objects` - объекты (id, container_id, name, description, status, photo, aikona_object_id, blocking_factors, created_at, updated_at)
- `acts` - акты по СМР (object_id, smr_id, smr_name, act_type, count, total)
- `tasks` - задачи разработки
- `snapshots` - снимки статистики

**Автоматическая миграция:**
- Система автоматически добавляет недостающие колонки при инициализации
- Поддержка миграции `display_order` и `aikona_object_id`

### Формат хранения объектов (objects.json)

```json
{
  "containers": [
    {
      "id": 1,
      "name": "Объекты",
      "objects": [
        {
          "id": 1,
          "name": "Название объекта",
          "description": "Секция/подрядчик",
          "status": "Текущий статус объекта",
          "photo": "base64_encoded_image_data",
          "aikonaObjectId": 401,
          "generatedActs": [
            {
              "id": 1,
              "name": "Внутренние стены",
              "count": 10,
              "total": 15
            }
          ],
          "sentForApproval": [...],
          "approvedActs": [...],
          "rejectedActs": [...],
          "signedActs": [...],
          "blockingFactors": ["Иванов И.И."],
          "createdAt": "2025-01-15T10:00:00.000Z",
          "updatedAt": "2025-01-15T10:00:00.000Z"
        }
      ]
    }
  ]
}
```

### Формат снимков (snapshots.json)

Массив объектов со структурой:
- `id` - уникальный идентификатор
- `date` - дата создания снимка (ISO строка)
- `type` - тип снимка ('meeting')
- `containers` - массив контейнеров с объектами и их СМР на момент снимка

### Формат задач (tasks.json)

Массив объектов со структурой:
- `id` - уникальный идентификатор
- `taskNumber` - номер задачи (число)
- `description` - описание задачи
- `discoveryDate` - дата обнаружения (ISO строка)
- `status` - статус: "To Do", "In Progress", "Review", "Done"
- `plannedFixMonth` - планируемый месяц исправления (строка "01"-"12" или null)
- `plannedFixYear` - планируемый год исправления (число или null)
- `priority` - критичность задачи: "critical", "non-critical", "user-request"
- `createdAt` - дата создания (ISO строка)
- `updatedAt` - дата обновления (ISO строка)

## ⌨️ Горячие клавиши

- **Insert** - Создать новый контейнер объектов
- **Двойной клик** на заголовке - Переименовать
- **Enter** - Сохранить изменения при редактировании
- **Escape** - Отменить изменения при редактировании
- **Delete** на СМР в модальном окне - Удалить СМР (с подтверждением)
- **Delete** на строке задачи - Удалить задачу (с подтверждением)

## 🔧 Особенности реализации

### Миграция данных

Система автоматически мигрирует старые данные (если они были в формате массива объектов) в новый формат с контейнерами.

### Валидация данных

- Количество отправленных на согласование актов не может превышать количество сгенерированных
- Сумма согласованных и отклоненных актов не может превышать количество отправленных на согласование
- Подписанных актов не может быть больше согласованных
- Валидация выполняется как на клиенте, так и на сервере

### Расчет процентов

Проценты рассчитываются автоматически:
- Отправлено % = (sentForApproval / generatedActs) × 100
- Согласовано % = (approvedActs / sentForApproval) × 100
- Отклонено % = (rejectedActs / sentForApproval) × 100
- Подписано % = (signedActs / approvedActs) × 100

### Синхронизация СМР

При добавлении нового СМР в секцию "Сгенерированных актов", он автоматически добавляется во все остальные секции (отправлено, согласовано, отклонено, подписано). Переименование СМР также синхронизируется между всеми секциями.

## 🌐 Деплой в production

Подробная инструкция по деплою на Render.com находится в файле [`DEPLOY.md`](DEPLOY.md).

### ⚠️ ВАЖНО: Работа с данными при деплое

**КРИТИЧЕСКИ ВАЖНО**: Перед каждым деплоем необходимо скачивать данные с продакшена, чтобы не потерять их!

Подробные инструкции:
- [`SAFE_DEPLOY_GUIDE.md`](SAFE_DEPLOY_GUIDE.md) - Правильный процесс безопасного деплоя
- [`QUICK_BACKUP_GUIDE.md`](QUICK_BACKUP_GUIDE.md) - Быстрый гайд по бэкапу данных
- [`scripts/download-production-data.js`](scripts/download-production-data.js) - Скрипт для скачивания данных

**Краткий процесс:**
1. Скачайте данные: `node scripts/download-production-data.js`
2. Закоммитьте данные отдельно: `git add backend/data/*.json && git commit -m "Backup: Данные"`
3. Запушьте: `git push`
4. Только потом деплойте изменения кода

### Краткая информация:

1. **React приложение собирается** командой `npm run build` в папку `frontend/build/`
2. **Express сервер раздает статические файлы** из этой папки в production
3. **API endpoints** работают на том же домене с префиксом `/api/*`
4. **На Render.com** деплоится один Web Service, который содержит и backend, и frontend
5. **Данные хранятся в JSON файлах** в папке `backend/data/` и должны быть закоммичены в репозиторий

### Основные команды для деплоя:

```bash
# Build команда для Render.com:
cd backend && npm install && cd ../frontend && npm install && npm run build

# Start команда:
cd backend && npm start
```

### Настройки Render.com:

- **Root Directory**: (пусто) - корень репозитория
- **Build Command**: `cd backend && npm install && cd ../frontend && npm install && npm run build`
- **Start Command**: `cd backend && npm start`
- **Environment Variables**: Не требуются (API использует относительные пути)

## 📝 История изменений

### Версия 2.4 (Текущая)

- ✅ **Аналитика исполнительности и система алертов**:
  - Расчет процента готовности актов для каждого объекта (готовность = сгенерировано / должно быть из Айконы)
  - Отображение отставания (сколько актов еще нужно сгенерировать)
  - Визуальные индикаторы статуса на карточках объектов (зеленый/желтый/красный)
  - Значок критического объекта (пульсирующая красная иконка) для объектов с готовностью < 70%
  - Кнопка "Проблемы" в хедере Dashboard с счетчиком критических объектов
  - Панель критических объектов с детальной информацией
  - Автоматическая подсветка проблемных секций при открытии объекта через панель проблем
  - Автоматическое раскрытие секции "Сгенерированных актов" для критических объектов

- ✅ **Улучшения UI/UX**:
  - Кастомные tooltips (белый текст на черном фоне, закругленные края) для всех подсказок
  - Tooltips для кнопок фото отображаются снизу, чтобы не обрезаться контейнером
  - Задачи для разработки свернуты по умолчанию (с кнопкой развернуть/свернуть)
  - При экспорте в PDF все задачи всегда развернуты
  - Возможность изменять фото объекта (не только удалять) с кастомными подсказками

- ✅ **Ссылка на таск-менеджер**: Добавлено поле для ссылки на задачу во внешней системе
  - Поле ввода в модальном окне создания/редактирования задачи
  - Кнопка со стрелкой в столбце "Действия" для открытия ссылки
  - Кнопка видна всем пользователям (не только админам)

- ✅ **Улучшения модального окна объекта**:
  - Автоматическая подсветка критических секций при открытии через панель проблем
  - Красная рамка и пульсирующая анимация для проблемных секций
  - Автоматическая прокрутка к проблемной секции

### Версия 2.3

- ✅ **Интеграция с Aikona API**: Автоматическая синхронизация данных о выполненных работах
  - Поле "ID объекта в Айконе" для привязки объектов к внешней системе
  - Кнопка "Забрать данные" для ручной синхронизации
  - Автоматическая ежедневная синхронизация в 7:00 (МСК)
  - Логирование результатов синхронизации с уведомлениями для админов
  - Обновление поля "СЗ Icona" (общее количество) из Aikona API
- ✅ **Перемещение контейнеров**: Кнопки "↑" и "↓" для изменения порядка отображения контейнеров
  - Порядок сохраняется в базе данных через поле `display_order`
  - Автоматическая миграция существующих данных
- ✅ **Кастомные модальные окна**: Замена всех стандартных `alert` и `confirm` на стилизованные компоненты
  - `ConfirmModal` для подтверждений (создание/удаление снимков, удаление задач, выход из системы)
  - `ToastNotification` для уведомлений (автоматически исчезают через 3 секунды)
  - Улучшенный UX с единообразным дизайном
- ✅ **Фото объектов**: Возможность загружать JPEG/PNG фото объектов
  - Отображение фото на карточке объекта над названием
  - Увеличение высоты карточки при наличии фото
  - Сохранение в base64 формате
- ✅ **Улучшения статистики планерок**:
  - Строка итогов в таблице изменений по контейнерам
  - Все цифры в диаграмме отображаются черным цветом
  - Итоги отображаются в PDF экспорте
- ✅ **Улучшения PDF экспорта**:
  - Индикатор прогресса генерации ("Идет генерация PDF X из Y...")
  - Скрытие страниц во время генерации
  - Каждый контейнер объектов на отдельной странице
  - Плитки объектов одинакового размера
  - Таблица задач занимает почти всю ширину страницы (16 строк на странице)
  - Правильное выравнивание заголовков таблицы
  - Заголовки "Дата обнаружения", "Статус", "Планируется устранить", "Критичность" выровнены по правому краю
- ✅ **Адаптивная верстка**: Поддержка различных разрешений экранов
  - Мобильные устройства: 360px (оптимизированы плитки, убраны лишние отступы)
  - Планшеты: 768px
  - Десктопы: 1040px, 1366px, 1920px
- ✅ **Улучшения модального окна объекта**:
  - Заголовки столбцов для СМР: "СМР", "Praktis ID", "СЗ Icona"
  - Исправлена логика закрытия модального окна (не закрывается при выделении текста и отпускании мыши вне окна)
  - Кнопка "Показать статистику" перенесена в заголовок контейнера
- ✅ **Исправления базы данных**:
  - Убрана ручная генерация ID для объектов (используется PostgreSQL SERIAL)
  - Исправлена проблема с созданием новых объектов и контейнеров
  - Автоматическая миграция схемы базы данных (добавление полей `display_order`, `aikona_object_id`)
- ✅ **Улучшения безопасности**:
  - Кнопка "Сохранить снимок" заблокирована в режиме просмотра
  - Удаление снимков доступно только авторизованным пользователям

### Версия 2.2

- ✅ Добавлена система аутентификации (логин/пароль, режим просмотра)
- ✅ Реализован экспорт в PDF с титульной страницей и многостраничной структурой
- ✅ Добавлена критичность задач (Criticality) с тремя уровнями и иконками
- ✅ Реализована сортировка задач по критичности
- ✅ Добавлено вертикальное выравнивание в PDF экспорте
- ✅ Улучшена работа с данными при деплое (скрипты для бэкапа)
- ✅ Добавлены скрипты для проверки и скачивания данных с продакшена

### Версия 2.0

- ✅ Добавлена статистика планерок с созданием снимков
- ✅ Добавлен график сравнения статистики между планерками
- ✅ Добавлена таблица изменений по контейнерам
- ✅ Добавлен раздел "Задачи для разработки"
- ✅ Добавлена карточка "Подписано" в статистику
- ✅ Добавлен статус объекта (краткий текст на карточке)
- ✅ Добавлены блокирующие факторы (проблемные сотрудники)
- ✅ Реализовано inline редактирование статуса и планируемой даты в задачах
- ✅ Добавлено удаление через клавишу Delete
- ✅ Добавлен расчет процента сгенерированных актов от общего количества

### Версия 1.0

- ✅ Реализована система контейнеров объектов
- ✅ Добавлена возможность переименования заголовков
- ✅ Добавлено создание контейнеров по клавише Insert
- ✅ Реализована детальная работа с СМР (виды работ)
- ✅ Добавлена раскрывающаяся статистика контейнеров
- ✅ Реализована общая статистика по всем контейнерам
- ✅ Добавлена валидация данных
- ✅ Автоматический расчет процентов

## 🔒 Безопасность

- **Хранение данных**: PostgreSQL база данных (рекомендуется) или JSON файлы (для локальной разработки)
- Валидация на клиенте и сервере
- CORS настроен для работы с фронтендом
- В production статические файлы раздаются через Express
- **Система аутентификации**: Режим просмотра по умолчанию, редактирование только после авторизации
- **Сессионное хранилище**: Состояние авторизации хранится в `sessionStorage` (сбрасывается при закрытии браузера)
- **Защита от случайных изменений**: Все функции редактирования отключены в режиме просмотра
- **Увеличенный лимит размера запросов**: 50MB для поддержки загрузки фото объектов (base64)

## 📄 Лицензия

Внутренний проект для мониторинга внедрения Praktis ID.

## 👥 Авторы

Разработано для SetlSoft / Setl Group

---

**Версия:** 2.4  
**Дата последнего обновления:** Январь 2025

---

## 🔗 Интеграция с Aikona API

Система поддерживает автоматическую синхронизацию данных о выполненных работах из внешней системы Aikona.

**Настройка:**
1. В карточке объекта введите "ID объекта в Айконе" (например, 401)
2. Нажмите кнопку "Забрать данные" для ручной синхронизации
3. Система автоматически найдет соответствующие СМР по названию и обновит поле "СЗ Icona" (общее количество)

**Автоматическая синхронизация:**
- Ежедневная синхронизация в 7:00 (МСК) через cron job
- Логирование результатов синхронизации
- Уведомления для админов через кнопку "Лог синхронизации" в хедере

**API Aikona:**
- Endpoint: `https://icona.setl.ru/rest_api/swagger/ui/index#/IntegrationObjectInfo`
- API Key: `f48941fd-ab51-4edd-b1f2-f202597c9920`
- Логика: Подсчет `Locations` где `SZCompletion === 100` для каждого СМР

## 📚 Дополнительная документация

- [`SAFE_DEPLOY_GUIDE.md`](SAFE_DEPLOY_GUIDE.md) - Безопасный процесс деплоя с сохранением данных
- [`QUICK_BACKUP_GUIDE.md`](QUICK_BACKUP_GUIDE.md) - Быстрое руководство по бэкапу данных
- [`DEPLOY.md`](DEPLOY.md) - Подробная инструкция по деплою на Render.com
- [`RESTORE_DATA_NOW.md`](RESTORE_DATA_NOW.md) - Инструкции по восстановлению данных (если потеряны)
- [`MIGRATE_LOCAL_DATA_TO_DB.md`](MIGRATE_LOCAL_DATA_TO_DB.md) - Миграция данных из JSON в PostgreSQL
- [`SETUP_DATABASE.md`](SETUP_DATABASE.md) - Настройка PostgreSQL на Render.com
- [`AIKONA_INTEGRATION_SUMMARY.md`](AIKONA_INTEGRATION_SUMMARY.md) - Сводка по интеграции с Aikona API
