# 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА: Render.com теряет данные при пробуждении

## Проблема

**Render.com НЕ сохраняет файловые изменения между пробуждениями!**

Когда Render.com засыпает (free tier) и просыпается:
- Все изменения в файловой системе ТЕРЯЮТСЯ
- Render восстанавливает файлы из git репозитория
- Если данные не были в git → они теряются

## ⚠️ ВАЖНОЕ ПОНИМАНИЕ

**Render.com - это эфемерная файловая система!**

- Файлы сохраняются только в git репозитории
- Локальные изменения на сервере НЕ сохраняются между деплоями/пробуждениями
- Каждое пробуждение = новый деплой из git

## ✅ ПРАВИЛЬНОЕ РЕШЕНИЕ

### Вариант 1: Всегда коммитить данные перед изменениями (РЕКОМЕНДУЕТСЯ)

**Процесс:**

1. **Перед работой на продакшене:**
   - Убедитесь, что последние данные уже в git
   - Скачайте данные: `node scripts/download-production-data.js`
   - Закоммитьте: `git add backend/data/*.json && git commit -m "Backup" && git push`

2. **Работаете на продакшене:**
   - Вносите изменения через интерфейс
   - Данные сохраняются во временные файлы на Render

3. **СРАЗУ после работы (до того, как Render заснет):**
   - Скачайте данные: `node scripts/download-production-data.js`
   - Проверьте: `node scripts/check-act-counts.js`
   - Закоммитьте и запушьте: `git add backend/data/*.json && git commit -m "Backup: Новые данные" && git push`

4. **Render заснет и проснется:**
   - Данные восстановятся из git ✅

### Вариант 2: Использовать внешнее хранилище (для production)

Для production рекомендуется использовать:
- **База данных** (PostgreSQL, MongoDB) - Render предоставляет managed databases
- **Объектное хранилище** (S3-совместимое хранилище)
- **Внешний git репозиторий** для данных (отдельный репозиторий)

---

## 🔍 Как проверить, что данные в git:

```bash
cd ID

# Проверить последний коммит с данными
git log --oneline -5 -- backend/data/objects.json

# Посмотреть, что в последнем коммите
git show HEAD:backend/data/objects.json | head -100

# Проверить статус (не должно быть изменений)
git status backend/data/
```

---

## 🚨 ЧТО ДЕЛАТЬ СЕЙЧАС:

1. **Проверьте, что данные в git:**
   ```bash
   git log --oneline -5 -- backend/data/objects.json
   git show HEAD:backend/data/objects.json | head -100
   ```

2. **Если данных нет в git:**
   - ❌ Данные потеряны безвозвратно
   - Нужно ввести заново

3. **Если данные есть в git:**
   - Проверьте, что они были запушены: `git log origin/main --oneline -5`
   - Если не запушены - запушьте: `git push`
   - Подождите, пока Render задеплоит изменения

---

## 📋 Чеклист для работы с данными на Render:

- [ ] Перед работой: данные закоммичены в git
- [ ] Работаете на продакшене (вносите изменения)
- [ ] **СРАЗУ после работы** (до того, как сервер заснет): скачать данные
- [ ] Проверить данные (скрипт check-act-counts.js)
- [ ] Закоммитить данные: `git add backend/data/*.json`
- [ ] Закоммитить: `git commit -m "Backup: Данные $(date)"`
- [ ] Запушить: `git push`
- [ ] Убедиться, что push успешен
- [ ] Теперь можно ждать, пока Render заснет/проснется

---

## 💡 Рекомендации:

1. **Всегда коммитить данные сразу после работы** на продакшене
2. **Не ждать**, пока Render заснет - коммитить сразу
3. **Использовать скрипты проверки** перед каждым коммитом
4. **Для production**: рассмотреть переход на базу данных

---

## ⚠️ ПОМНИТЕ:

**Render.com = эфемерная файловая система!**

- ✅ Сохраняются только данные из git
- ❌ Локальные изменения на сервере теряются

**Единственный источник истины = git репозиторий!**

