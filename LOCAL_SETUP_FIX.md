# 🔧 Исправление проблемы с пустыми данными на localhost

## Проблема

При запуске на localhost все пусто - нет объектов и задач.

**Причина:** В файле `.env` установлена `DATABASE_URL` с дефолтным значением, и система пытается подключиться к PostgreSQL, которого нет локально.

## ✅ Решение

### Вариант 1: Использовать JSON файлы (рекомендуется для локальной разработки)

1. Откройте файл `ID/backend/.env`
2. Закомментируйте или удалите строку с `DATABASE_URL`:
   ```env
   # DATABASE_URL=postgresql://user:password@localhost:5432/dbname
   ```
   
   Или полностью удалите эту строку.

3. Перезапустите сервер

Теперь система будет использовать JSON файлы из папки `ID/backend/data/`, где уже есть ваши данные.

---

### Вариант 2: Использовать реальную PostgreSQL базу данных

Если у вас установлен PostgreSQL локально:

1. Создайте базу данных
2. Обновите `DATABASE_URL` в `.env`:
   ```env
   DATABASE_URL=postgresql://username:password@localhost:5432/dbname
   ```
3. Запустите миграцию (если нужно):
   ```bash
   node scripts/migrate-local-data.js
   ```

---

## 📝 Быстрое исправление

**Просто удалите или закомментируйте строку `DATABASE_URL` в `ID/backend/.env`:**

⚠️ **ВАЖНО:** Оставьте эту строку закомментированной! Для продакшена на Render.com переменные окружения настраиваются через интерфейс, а не через `.env` файл.

```env
# Database Configuration
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname  <-- Закомментируйте эту строку

# Aikona API Configuration
AIKONA_API_KEY=f48941fd-ab51-4edd-b1f2-f202597c9920

# Admin Authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin2026!

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-to-random-string-in-production

# Server Configuration
PORT=3001
```

После этого перезапустите сервер, и все данные из JSON файлов должны появиться!

