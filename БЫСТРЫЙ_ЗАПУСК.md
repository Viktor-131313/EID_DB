# 🚀 БЫСТРЫЙ ЗАПУСК Praktis ID Dashboard

## ✅ Бэкенд уже запущен!

Бэкенд работает на `http://localhost:3001`

## 📝 Что делать дальше:

### 1. Обновите страницу в браузере (F5 или Ctrl+R)

После обновления ошибки 404 должны исчезнуть, и дашборд заработает!

---

## 🔄 Как запускать в будущем:

### Вариант 1: Два отдельных терминала (рекомендуется)

**Терминал 1 - Бэкенд:**
```bash
cd ID/backend
node server.js
```

**Терминал 2 - Фронтенд:**
```bash
cd ID/frontend
npm start
```

### Вариант 2: Использовать готовые скрипты

**Windows (PowerShell):**
```powershell
# Бэкенд
cd ID
.\start-backend.ps1

# Фронтенд (в другом терминале)
cd ID/frontend
npm start
```

**Windows (CMD):**
```cmd
# Бэкенд
cd ID
start-backend.bat

# Фронтенд (в другом терминале)
cd ID/frontend
npm start
```

---

## ⚠️ Важно:

1. **Бэкенд должен быть запущен ДО фронтенда**
2. Бэкенд работает на порту **3001**
3. Фронтенд работает на порту **3000**
4. Если видите ошибки 404 - проверьте, что бэкенд запущен

---

## ✅ Проверка работы:

1. Откройте `http://localhost:3001/api/health` в браузере
   - Должно показать: `{"status":"ok","timestamp":"..."}`

2. Обновите страницу дашборда (`http://localhost:3000`)
   - Ошибки должны исчезнуть
   - Можно добавлять объекты

---

## 🐛 Устранение проблем:

**Ошибка 404 для /api/objects или /api/stats:**
- Бэкенд не запущен → запустите бэкенд (см. выше)

**Ошибка "Port 3001 already in use":**
- Порт занят → закройте другой процесс или перезапустите бэкенд

**Ошибка "ECONNREFUSED":**
- Бэкенд не запущен → запустите бэкенд

