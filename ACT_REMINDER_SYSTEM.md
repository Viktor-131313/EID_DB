# 🔔 Система напоминаний о несогласованных актах

## 📋 Описание проблемы

Когда акт исполнительной документации сгенерирован в Практис ID и отправлен на согласование пользователю, требуется система автоматических напоминаний:
- Если акт находится на согласовании длительное время без действий (например, сутки)
- Пользователь должен получить напоминание о необходимости обработать акт
- Руководство также должно быть уведомлено о задержке согласования

## 🎯 Цель

Автоматизировать процесс отслеживания актов, находящихся на согласовании, и своевременно информировать ответственных лиц о необходимости принятия решения.

---

## 📊 Текущая ситуация

### Что есть сейчас:

1. **Интеграция с Aikona API**:
   - Получаем данные о выполненных работах
   - Обновляем поле "СЗ Icona" (общее количество актов, которые должны быть сгенерированы)
   - Отслеживаем количество сгенерированных актов (Praktis ID)

2. **Ручное управление**:
   - Пользователи вручную вводят количество сгенерированных актов
   - Вручную проставляют количество отправленных на согласование
   - Вручную отслеживают статусы согласования

### Что отсутствует:

- ❌ API Практис ID для получения информации о статусах актов
- ❌ Интеграция с системой уведомлений (email/telegram/sms)
- ❌ Автоматическое отслеживание времени нахождения акта на согласовании
- ❌ Система напоминаний для пользователей и руководства

---

## 🔄 Как должна работать система напоминаний

### Этап 1: Получение данных из Практис ID (после получения API)

#### 1.1. Синхронизация актов

```
Практис ID API → Дашборд
├── Список всех актов
├── ID акта
├── ID объекта
├── ID СМР
├── Дата создания акта
├── Дата отправки на согласование
├── Текущий статус (черновик/на согласовании/согласован/отклонен)
├── ID пользователя-согласующего
└── Последняя активность (дата/время последнего действия)
```

#### 1.2. Отслеживание времени на согласовании

Для каждого акта, находящегося на согласовании, система отслеживает:

- **Время на согласовании** = Текущая дата/время - Дата отправки на согласование
- **Статус активности** = Есть ли действия за последние N часов (например, 24 часа)

---

### Этап 2: Правила отправки напоминаний

#### 2.1. Условия для напоминания пользователю

Напоминание отправляется пользователю-согласующему, если:

1. ✅ Акты находятся на согласовании более **24 часов** (настраиваемое значение)
2. ✅ За последние 24 часа не было никаких действий с актами
3. ✅ Статус акта = "На согласовании"
4. ✅ Пользователь не в отпуске (опционально, если есть интеграция с HR)

#### 2.2. Условия для уведомления непосредственного руководителя (Тип 2)

Непосредственный руководитель получает уведомление, если:

1. ✅ Акты находятся на согласовании более **48 часов** (настраиваемое значение)
2. ✅ Пользователь уже получил напоминание (не менее 24 часов назад)
3. ✅ Статус акта все еще = "На согласовании"
4. ✅ Критичность акта = "Высокая" (опционально)

#### 2.3. Условия для эскалации высшему руководству (Тип 3)

Высшее руководство получает уведомление, если:

1. ✅ Акты находятся на согласовании более **72 часов** (настраиваемое значение)
2. ✅ Непосредственный руководитель уже получил уведомление (не менее 24 часов назад)
3. ✅ Статус акта все еще = "На согласовании"
4. ✅ Это финальное уведомление в рамках автоматической системы эскалации

---

### Этап 3: Формат напоминаний

#### 3.1. Напоминание пользователю (Тип 1)

**Канал отправки**: Email / Telegram / SMS (настраиваемо)

**Тема письма**: 
```
Требуется ваше действие на платформе Praktis ID
```

**Содержание письма**:
```
Добрый день, [Имя Отчество пользователя]!

Напоминаем вам, что на платформе Praktis ID ожидают вашего внимания документы (акты), 
требующие рассмотрения и согласования.

Пожалуйста, зайдите на платформу в течение ближайших 24 часов для выполнения 
необходимых действий.

Обращаем ваше внимание, что через 48 часов с момента данного уведомления автоматическое 
оповещение будет направлено вашему непосредственному руководителю, 
[Имя Отчество руководителя, должность].

Данное сообщение сформировано и отправлено автоматически. Ответ на него не требуется.
```

#### 3.2. Уведомление непосредственному руководителю (Тип 2)

**Канал отправки**: Email / Telegram

**Тема письма**: 
```
Информация о действиях подчинённого на платформе Praktis ID
```

**Содержание письма**:
```
Добрый день, [Имя Отчество руководителя]!

Информируем вас, что ваш сотрудник, [Имя Отчество сотрудника, должность], 
на текущий момент не выполнил необходимые действия по рассмотрению и согласованию 
актов на платформе Praktis ID. Документы поступили ему [дата и время поступления].

Пожалуйста, примите, по возможности, меры для ускорения процесса согласования.

Для сведения: через 24 часа система автоматически проинформирует об этой ситуации 
вышестоящее руководство.

Данное сообщение сформировано и отправлено автоматически. Ответ на него не требуется.
```

#### 3.3. Эскалация высшему руководству (Тип 3)

**Канал отправки**: Email / Telegram

**Тема письма**: 
```
Эскалация: срыв сроков согласования актов в Praktis ID
```

**Содержание письма**:
```
Уважаемый(ая) [Имя Отчество высшего руководителя]!

Ставим вас в известность о следующем:
Сотрудник [Имя Отчество сотрудника, должность] не согласовал в установленные сроки 
акты на платформе Praktis ID.

Дата и время поступления документов сотруднику: [дата и время].

24 часа назад, [дата и время], автоматическое уведомление было направлено его 
непосредственному руководителю, [Имя Отчество руководителя, должность].

На сегодняшний день, [текущая дата], указанные акты остаются не согласованными, 
что может влиять на последующие процессы.

Данное сообщение является финальным уведомлением в рамках автоматической системы эскалации.

Данное сообщение сформировано и отправлено автоматически. Ответ на него не требуется.
```

---

### Этап 4: Частота напоминаний и эскалация

#### 4.1. Временная шкала эскалации

```
0 часов    → Акты отправлены на согласование
24 часа    → Напоминание пользователю (Тип 1)
48 часов   → Уведомление непосредственному руководителю (Тип 2)
72 часа    → Эскалация высшему руководству (Тип 3)
```

#### 4.2. Пользователю (Тип 1)

- **Первое напоминание**: Через 24 часа после отправки на согласование
- **Условие**: За последние 24 часа не было действий с актами
- **Содержание**: Информирует о необходимости действий и предупреждает об эскалации через 48 часов

#### 4.3. Непосредственному руководителю (Тип 2)

- **Первое уведомление**: Через 48 часов (после первого напоминания пользователю)
- **Условие**: Пользователь уже получил напоминание (не менее 24 часов назад)
- **Содержание**: Информирует о ситуации и предупреждает об эскалации высшему руководству через 24 часа

#### 4.4. Высшему руководству (Тип 3)

- **Эскалация**: Через 72 часа (после уведомления непосредственного руководителя)
- **Условие**: Руководитель уже получил уведомление (не менее 24 часов назад)
- **Содержание**: Финальное уведомление в рамках автоматической системы эскалации

---

### Этап 5: Настройки системы

#### 5.1. Конфигурируемые параметры

В настройках дашборда (для админа) должны быть доступны:

```json
{
  "reminderSettings": {
    "userReminderInterval": 24,  // часов до первого напоминания пользователю
    "managerNotificationInterval": 48,  // часов до уведомления руководства
    "repeatReminderInterval": 24,  // часов между повторными напоминаниями
    "maxUserReminders": 3,  // максимальное количество напоминаний пользователю
    "criticalActHours": 12,  // для критичных актов - напоминание через 12 часов
    "enabledChannels": ["email", "telegram"],  // каналы отправки
    "emailTemplates": {
      "userReminder": "template_user_reminder.html",
      "managerNotification": "template_manager_notification.html"
    }
  },
  "notificationRecipients": {
    "managers": ["manager1@company.com", "manager2@company.com"],
    "defaultUserEmailDomain": "@company.com"
  }
}
```

#### 5.2. Интеграция с каналами отправки

**Email**:
- SMTP сервер для отправки писем
- Шаблоны писем (HTML)
- Настройка отправителя (noreply@company.com)

**Telegram**:
- Telegram Bot API
- Чат-бот для отправки уведомлений
- Групповые чаты для руководства

**SMS** (опционально):
- SMS-шлюз для критичных напоминаний
- Только для экстренных случаев

---

## 🔧 Техническая реализация (после получения API)

### Компоненты системы

#### 1. Модуль синхронизации с Практис ID API

```javascript
// services/praktis-api-sync.js
class PraktisAPISync {
  // Получение списка актов на согласовании
  async getActsOnApproval() {
    // Запрос к Практис ID API
    // Фильтрация по статусу "на согласовании"
    // Возврат массива актов с метаданными
  }

  // Получение истории действий с актом
  async getActHistory(actId) {
    // Получение лога всех действий с актом
    // Определение последней активности
  }
}
```

#### 2. Модуль отслеживания времени на согласовании

```javascript
// services/act-time-tracker.js
class ActTimeTracker {
  // Расчет времени на согласовании
  calculateTimeOnApproval(act) {
    const sentDate = new Date(act.sentForApprovalDate);
    const now = new Date();
    return Math.floor((now - sentDate) / (1000 * 60 * 60)); // в часах
  }

  // Проверка необходимости напоминания
  shouldSendReminder(act, reminderSettings) {
    const hoursOnApproval = this.calculateTimeOnApproval(act);
    const lastActivity = this.getLastActivityTime(act);
    const hoursSinceActivity = this.calculateHoursSince(lastActivity);

    return (
      hoursOnApproval >= reminderSettings.userReminderInterval &&
      hoursSinceActivity >= reminderSettings.userReminderInterval
    );
  }
}
```

#### 3. Модуль отправки уведомлений

```javascript
// services/notification-service.js
class NotificationService {
  // Отправка напоминания пользователю
  async sendUserReminder(act, user) {
    const email = user.email || `${user.login}@company.com`;
    const template = this.loadTemplate('userReminder');
    
    await this.sendEmail({
      to: email,
      subject: `Напоминание: Требуется согласование актов - ${act.objectName}`,
      body: this.renderTemplate(template, { act, user })
    });

    // Логирование отправки
    await this.logNotification('user_reminder', act.id, user.id);
  }

  // Отправка уведомления руководству
  async sendManagerNotification(act, user) {
    const managers = this.getManagerEmails();
    const template = this.loadTemplate('managerNotification');
    
    for (const managerEmail of managers) {
      await this.sendEmail({
        to: managerEmail,
        subject: `Уведомление: Задержка согласования актов - ${act.objectName}`,
        body: this.renderTemplate(template, { act, user })
      });
    }

    // Логирование отправки
    await this.logNotification('manager_notification', act.id, user.id);
  }
}
```

#### 4. Планировщик задач (Cron Job)

```javascript
// services/reminder-scheduler.js
const cron = require('node-cron');

class ReminderScheduler {
  // Запуск проверки каждые 2 часа
  start() {
    cron.schedule('0 */2 * * *', async () => {
      await this.checkAndSendReminders();
    });
  }

  async checkAndSendReminders() {
    // 1. Синхронизация актов из Практис ID
    const acts = await praktisAPI.getActsOnApproval();
    
    // 2. Проверка каждого акта
    for (const act of acts) {
      const shouldRemind = timeTracker.shouldSendReminder(act, settings);
      
      if (shouldRemind) {
        // 3. Проверка истории отправок (чтобы не спамить)
        const lastReminder = await this.getLastReminder(act.id);
        const canSend = this.canSendReminder(lastReminder, settings);
        
        if (canSend) {
          // 4. Отправка напоминания пользователю
          const user = await this.getUserById(act.approverId);
          await notificationService.sendUserReminder(act, user);
          
          // 5. Проверка необходимости уведомления руководства
          if (this.shouldNotifyManager(act, settings)) {
            await notificationService.sendManagerNotification(act, user);
          }
        }
      }
    }
  }
}
```

---

## 📊 Хранение данных в базе

### Таблица `act_reminders`

```sql
CREATE TABLE act_reminders (
  id BIGSERIAL PRIMARY KEY,
  act_id VARCHAR(255) NOT NULL,  -- ID акта из Практис ID
  object_id INTEGER REFERENCES objects(id),
  smr_id INTEGER,
  approver_id INTEGER,  -- ID пользователя-согласующего
  sent_for_approval_date TIMESTAMP NOT NULL,
  first_reminder_sent_at TIMESTAMP,
  last_reminder_sent_at TIMESTAMP,
  reminder_count INTEGER DEFAULT 0,  -- количество отправленных напоминаний
  manager_notification_sent_at TIMESTAMP,
  manager_notification_count INTEGER DEFAULT 0,
  status VARCHAR(50) DEFAULT 'pending',  -- pending, resolved, escalated
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица `notification_logs`

```sql
CREATE TABLE notification_logs (
  id BIGSERIAL PRIMARY KEY,
  act_reminder_id INTEGER REFERENCES act_reminders(id),
  notification_type VARCHAR(50),  -- user_reminder, manager_notification
  recipient_email VARCHAR(255),
  recipient_type VARCHAR(50),  -- user, manager
  sent_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  delivery_status VARCHAR(50),  -- sent, delivered, failed
  error_message TEXT
);
```

---

## 🎨 UI в дашборде

### Раздел "Напоминания" (для админа)

**Страница настроек напоминаний** (`/admin/reminders`):

1. **Настройки интервалов**:
   - Ползунки для настройки времени до первого напоминания
   - Настройка интервалов повторных напоминаний
   - Выбор каналов отправки (чекбоксы: Email, Telegram, SMS)

2. **Список активных напоминаний**:
   - Таблица с актами на согласовании
   - Время на согласовании
   - Количество отправленных напоминаний
   - Статус (ожидает, напоминание отправлено, уведомление руководству)

3. **История напоминаний**:
   - Лог всех отправленных напоминаний
   - Статусы доставки
   - Возможность повторной отправки вручную

### Виджет на главной странице

**Блок "Требуют внимания"**:
- Количество актов, которые скоро потребуют напоминания (через 4 часа)
- Количество актов, по которым уже отправлены напоминания
- Ссылка на детальную информацию

---

## ✅ Критерии успеха

Система будет считаться успешно реализованной, если:

1. ✅ Автоматически отслеживает все акты на согласовании
2. ✅ Отправляет напоминания пользователям через заданные интервалы
3. ✅ Уведомляет руководство о задержках
4. ✅ Не спамит (контроль частоты отправки)
5. ✅ Логирует все отправки для аудита
6. ✅ Предоставляет админу возможность настройки параметров
7. ✅ Интегрируется с Практис ID API (когда будет доступно)
8. ✅ Поддерживает несколько каналов отправки (email, telegram)

---

## 🚀 План реализации (после получения API)

### Фаза 1: Подготовка (1-2 дня)
- [ ] Изучение Практис ID API документации
- [ ] Проектирование структуры данных
- [ ] Создание миграций базы данных

### Фаза 2: Базовый функционал (3-5 дней)
- [ ] Реализация синхронизации с Практис ID API
- [ ] Модуль отслеживания времени на согласовании
- [ ] Базовая логика отправки напоминаний (email)
- [ ] Планировщик задач (cron job)

### Фаза 3: UI и настройки (2-3 дня)
- [ ] Страница настроек напоминаний
- [ ] Список активных напоминаний
- [ ] История отправок
- [ ] Виджет на главной странице

### Фаза 4: Расширенный функционал (2-3 дня)
- [ ] Интеграция с Telegram
- [ ] Шаблоны писем (HTML)
- [ ] Дополнительные настройки (критичность, каналы)
- [ ] Тестирование и отладка

### Фаза 5: Тестирование и запуск (1-2 дня)
- [ ] Тестирование на тестовых данных
- [ ] Проверка доставки уведомлений
- [ ] Настройка на production
- [ ] Мониторинг первых дней работы

---

## 📝 Примечания

1. **До получения API**: Система не может быть реализована полностью, так как требуется доступ к данным Практис ID о статусах актов и пользователях-согласующих.

2. **Альтернативный вариант**: Можно реализовать упрощенную версию на основе данных, которые пользователи вручную вводят в дашборд (количество отправленных на согласование), но это будет менее точным.

3. **Безопасность**: При реализации необходимо обеспечить безопасность хранения данных о пользователях и их email/telegram ID.

4. **Масштабируемость**: Система должна корректно работать при большом количестве актов (сотни/тысячи актов на согласовании одновременно).

---

**Дата создания документа**: Январь 2025  
**Статус**: Концепция (ожидание API Практис ID)  
**Версия**: 1.0

