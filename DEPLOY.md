# 🚀 Инструкция по деплою на Render.com

Это руководство поможет вам развернуть Praktis ID Dashboard на Render.com.

## 📋 Содержание

1. [Как работает React приложение](#как-работает-react-приложение)
2. [Подготовка проекта для деплоя](#подготовка-проекта-для-деплоя)
3. [Деплой на Render.com](#деплой-на-rendercom)
4. [Настройка Cron Job](#настройка-cron-job)
5. [Обновление проекта](#обновление-проекта)

---

## 🎓 Как работает React приложение

### В разработке (локально)

Когда вы запускаете `npm start` во фронтенде:
- React запускает **dev-сервер** на `localhost:3000`
- Браузер загружает JavaScript код, который компилируется **на лету**
- Код отправляет запросы к backend на `localhost:3001`
- При изменении кода страница **автоматически обновляется** (Hot Reload)

### В production (на сервере)

React приложение нужно **собрать** (build):
- Команда `npm run build` создает оптимизированные статические файлы (HTML, CSS, JS)
- Эти файлы помещаются в папку `frontend/build/`
- Express сервер **раздает** эти статические файлы пользователям
- Все запросы к API идут на тот же сервер, но на маршруты `/api/*`

### Схема работы в production:

```
Пользователь → https://ваш-проект.onrender.com
                     ↓
            Express сервер
                     ↓
        ┌────────────┴────────────┐
        ↓                         ↓
   Статические файлы          API запросы
   (React build)              (/api/*)
   /index.html
   /static/js/...
   /static/css/...
```

---

## 📦 Подготовка проекта для деплоя

### 1. Создайте файл `.env` в корне проекта (опционально)

Для локальной разработки можно создать `.env` файл, но **не коммитьте его в Git**:

```env
NODE_ENV=development
PORT=3001
```

### 2. Обновите API URL для production

В файле `frontend/src/services/api-containers.js` и `frontend/src/services/api-tasks.js` уже используется переменная окружения:

```javascript
const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:3001/api';
```

На Render.com переменную `REACT_APP_API_URL` можно не задавать, так как API и фронтенд будут на одном домене.

### 3. Создайте скрипт для сборки (опционально)

В корне проекта можно создать `build.sh` для локальной сборки:

```bash
#!/bin/bash
cd frontend
npm install
npm run build
cd ..
```

---

## 🌐 Деплой на Render.com

### Шаг 1: Подготовьте GitHub репозиторий

1. Создайте новый репозиторий на GitHub (например, `praktis-id-dashboard`)
2. Загрузите код:

```bash
cd ID
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/praktis-id-dashboard.git
git push -u origin main
```

**Важно:** Убедитесь, что файл `.gitignore` присутствует и `node_modules/` не коммитится.

### Шаг 2: Создайте Web Service на Render.com

1. Зайдите на [render.com](https://render.com) и зарегистрируйтесь (можно через GitHub)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Настройте сервис:

   - **Name:** `praktis-id-dashboard` (или любое другое имя)
   - **Region:** `Frankfurt` (ближайший к России)
   - **Branch:** `main`
   - **Root Directory:** `ID` (если репозиторий содержит папку ID) или оставьте пустым если код в корне
   - **Runtime:** `Node`
   - **Build Command:** 
     ```bash
     cd backend && npm install && cd ../frontend && npm install && npm run build
     ```
   - **Start Command:**
     ```bash
     cd backend && npm start
     ```
   - **Instance Type:** `Free` (для начала)

5. Нажмите **"Create Web Service"**

### Шаг 3: Настройте переменные окружения

В настройках сервиса (Settings → Environment) добавьте:

- **Key:** `NODE_ENV` → **Value:** `production`
- **Key:** `PORT` → оставьте пустым (Render автоматически назначит порт через `process.env.PORT`)

**Опционально:**
- Если хотите использовать другой URL для API (если фронт и бэк на разных сервисах):
  - **Key:** `REACT_APP_API_URL` → **Value:** `https://ваш-backend.onrender.com/api`

### Шаг 4: Дождитесь деплоя

- Первый деплой займет 5-10 минут
- Вы увидите логи сборки и запуска
- После успешного деплоя вы получите URL: `https://praktis-id-dashboard.onrender.com`

---

## ⏰ Настройка Cron Job

Render.com позволяет создать **Scheduled Job** (Cron Job) для периодических задач.

### Вариант 1: Cron Job для "пробуждения" сервиса

Бесплатные сервисы на Render.com "засыпают" после 15 минут неактивности. Cron Job может "будить" сервис.

1. В Dashboard Render.com нажмите **"New +"** → **"Scheduled Job"**
2. Настройте:
   - **Name:** `wake-up-service`
   - **Region:** тот же, что у Web Service
   - **Schedule:** `*/14 * * * *` (каждые 14 минут)
   - **Start Command:**
     ```bash
     curl -X GET https://ваш-проект.onrender.com/api/health
     ```

Это будет отправлять запрос каждые 14 минут, чтобы сервис не засыпал.

### Вариант 2: Cron Job для выполнения задач на сервере

Если нужно выполнять какие-то задачи на сервере (например, создание автоматических снимков):

1. Создайте отдельный endpoint в `backend/server.js`:
   ```javascript
   app.get('/api/cron/daily-snapshot', async (req, res) => {
       // Ваша логика
       // Проверка авторизации через заголовок (для безопасности)
       const authHeader = req.headers['x-cron-auth'];
       if (authHeader !== process.env.CRON_SECRET) {
           return res.status(401).json({ error: 'Unauthorized' });
       }
       // ... ваш код
   });
   ```

2. Создайте Scheduled Job:
   - **Schedule:** `0 9 * * *` (каждый день в 9:00 UTC)
   - **Start Command:**
     ```bash
     curl -X GET https://ваш-проект.onrender.com/api/cron/daily-snapshot -H "x-cron-auth: ваш-секретный-ключ"
     ```

3. Добавьте переменную окружения `CRON_SECRET` в настройках сервиса.

---

## 🔄 Обновление проекта

### Автоматический деплой

Render.com автоматически передеплоит проект при каждом `git push` в основную ветку:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Render автоматически:
1. Обнаружит изменения
2. Запустит Build Command
3. Перезапустит сервис с новым кодом

### Ручной деплой

В Dashboard Render.com можно нажать **"Manual Deploy"** → **"Deploy latest commit"**.

---

## 🔧 Решение проблем

### Проблема: Сервис не запускается

**Проверьте логи:**
1. В Dashboard Render.com откройте ваш сервис
2. Перейдите на вкладку **"Logs"**
3. Найдите ошибки (красным цветом)

**Частые ошибки:**
- `npm install` не прошел → проверьте `package.json`
- Порт не указан → убедитесь, что используется `process.env.PORT`
- Путь к файлам неверный → используйте относительные пути

### Проблема: Frontend не загружается

**Проверьте:**
1. Выполнился ли `npm run build` (смотрите логи)
2. Существует ли папка `frontend/build`
3. Правильно ли настроен путь в `server.js` к статике

### Проблема: API запросы не работают

**Проверьте:**
1. CORS настроен правильно (`app.use(cors())`)
2. API маршруты начинаются с `/api/`
3. В production не используйте `localhost:3001`, используйте относительные пути или переменную окружения

### Проблема: Данные не сохраняются

**Важно:** На бесплатном тарифе Render.com файлы в файловой системе **не сохраняются** после перезапуска!

**Решения:**
1. Используйте бесплатную базу данных (PostgreSQL на Render.com, MongoDB Atlas)
2. Переключитесь на платный тариф (хранилище сохраняется)
3. Используйте внешнее хранилище (S3, Cloudinary)

Для тестирования можно оставить JSON файлы, но помните, что данные могут пропасть.

---

## 📝 Чеклист перед деплоем

- [ ] Код загружен в GitHub
- [ ] `.gitignore` настроен правильно
- [ ] `package.json` в backend и frontend корректен
- [ ] Build команда протестирована локально
- [ ] Переменные окружения настроены
- [ ] `server.js` настроен на раздачу статики в production
- [ ] Используется `process.env.PORT` для порта
- [ ] CORS настроен
- [ ] Все зависимости указаны в `package.json`

---

## 🔗 Полезные ссылки

- [Документация Render.com](https://render.com/docs)
- [React Production Build](https://create-react-app.dev/docs/production-build/)
- [Express Static Files](https://expressjs.com/en/starter/static-files.html)

---

**Удачи с деплоем! 🚀**

